{
  "name": "anniversary-plush",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --no-warnings=ExperimentalWarning --test \"tests/**/*.test.js\"",
    "test:db": "node --no-warnings=ExperimentalWarning --test \"tests/db/*.test.js\"",
    "serve": "python3 -m http.server 8000"
  }
}
