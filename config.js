// Public configuration. The Supabase anon key is designed to be public; RLS is the security boundary.
// Leave SUPABASE_URL empty to run Plush with the local (single-device) store.
export default {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
  SITE_URL: "",
  VAPID_PUBLIC_KEY: "",
};
